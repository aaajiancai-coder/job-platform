<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.job.mapper.NotificationMapper">
    <select id="selectByUserIdPaged" resultType="com.job.entity.Notification">
        SELECT * FROM notifications WHERE user_id = #{userId} ORDER BY created_at DESC LIMIT #{offset}, #{pageSize}
    </select>
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId}
    </select>
</mapper>
